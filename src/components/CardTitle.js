import React from "react";

const CardTitle = () => (
  <input
    type="text"
    name="card-title"
    className="card-title"
    placeholder="Input Title Here..."
  />
);

export default CardTitle;
